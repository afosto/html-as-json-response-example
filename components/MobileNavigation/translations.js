import { defineMessages } from 'react-intl';

export default defineMessages({
  rootPanelLabel: {
    id: 'mobileNavigation.root.label',
    defaultMessage: 'the menu',
    description: 'Label of the root panel in the mobile navigation',
  },
});
