import { defineMessages } from 'react-intl';

export default defineMessages({
  labelPerPage: {
    id: 'pagination.itemsPerPage.options.label',
    defaultMessage: '{count} per page',
    description: 'Label of the items per page options',
  },
});
